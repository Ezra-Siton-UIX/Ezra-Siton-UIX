<!DOCTYPE html>
<html dir="{{ global.direction }}" lang="{{ global.language }}" class="{{ global.direction }}">
<head>
	<!-- meta -->
	{{>meta}}
	<!-- SEO -->
	<title>{{seo.title}} | {{global.company_name}}</title>
	<meta name="description" content="{{ seo.seo_description }}">
	<meta name="keywords" content="{{ seo.seo_keywords }}">
	<meta name="author" content="Ezra Siton">

	<!-- FB OG -->
	<meta property="og:url" content="https://ezra-siton-design.netlify.com" />
	<meta property="og:type" content="website" />
	<meta property="og:title" content="{{seo.title}} | {{global.company_name}}" />
	<meta property="og:description" content="{{ seo.seo_description }}" />
	<meta property="og:image" content="{{ inside_header_img }}"/>

	<!-- UIkit CSS    -->
	<link rel="stylesheet" href="{{global.CSS}}" />
	<!-- CSS CUSTOM -->
	<link rel="stylesheet" href="/assets/css/main.css">

	<style type="text/css">
	{{>font-face}}
	{{>clipboard-css}}

	/* vegas */
	.vegas-timer {
		height: 5px!important;
		background: #04040440;
	}

	.vegas-timer-progress {
		background: #1e87f0!important;
	}

	/* red heading -- line */
	.uk-heading-bullet-red::before {
		content: "";
		display: inline-block;
		position: relative;
		top: calc(50% - 1px);
		vertical-align: middle;
		height: 1px;
		margin-{{global.left_on_rtl_right_on_ltr}}: 10px;
		border-{{global.left_on_rtl_right_on_ltr}}: 15px solid #FF4652;
	}

	#nav-mobile .uk-nav-sub li.uk-active a {
		color: #1e87f0;
	}


	html.rtl #en-button,  html.rtl #off-canvas-en-button{
		font-weight: light;
	}
	html.ltr #heb-butto, html.ltr #off-canvas-heb-button {
		font-weight: light;
	}

	html.rtl #off-canvas-heb-button {
		color: white!important;
	}
	html.ltr #off-canvas-en-button {
		color: white!important;
	}

	@media screen and (min-width: 959px) {
		html.rtl #heb-button, html.ltr #en-button {
			color: black!important;
		}

		html.rtl .transperent-nav #heb-button, html.ltr .transperent-nav #en-button {
			color: #1e87f0!important;
		}


	}
	/* uk-breadcrumb */
	.uk-breadcrumb>*>*{
		font-size: .8rem;
		color: #1e87f0;
	}
	.uk-breadcrumb>:nth-child(n+2):not(.uk-first-column)::before {
		margin: 0 10px;
	}
	/* responsive youtube */
	.embed-container { position: relative; padding-bottom: 50.25%; height: 0; overflow: visible;; max-width: 100%;} .embed-container iframe, .embed-container object, .embed-container embed { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }

	/* footer - uk-accordion-title hover */
	.uk-accordion-title:hover{
		background: rgba(51, 51, 51, 0.02);
		cursor: pointer;
	}

	/* ############ transperent NAVBAR ###############*/
	/* remove border if the nav is transperent */
	.transperent-nav .st-border-bottom{
		box-sizing:	border-box;
		-webkit-box-shadow: 0px 0px 0px 0px #f0f0f0;
		-moz-box-shadow: 0px 0px 0px 0px #f0f0f0;
		box-shadow: 0px 0px 0px 0px #f0f0f0;
	}

	.transperent-nav .uk-navbar-nav > li > a{
		color :rgba(255, 255, 255, 0.7);
	}

	.transperent-nav .uk-navbar-nav > li.uk-active > a{
		color :rgba(255, 255, 255, 1);

	}
	.uk-navbar-nav > li.uk-active > a{

		font-weight: bold;
	}
	.transperent-nav #mobile-logo a span, .transperent-nav #toogle a{
		color: white;
	}

	.st-big-z-index{
		z-index: 9999;
		position: relative;

	}
	@media screen and (max-width: 959px) {
		.uk-text-lead{
			font-size: 1.1rem;
			line-height: 1.65;
		}
	}

	@media screen and (max-width: 959px) {
		.uk-heading-primary{
			font-size: 1.9rem;
			line-height: 1.2;
		}
		.uk-h2, h2{
			font-size: 1.4rem;
			line-height: 1.2;
		}
	}
	</style> 
	{{>google_tag_manager}}
